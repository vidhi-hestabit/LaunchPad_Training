version: "3.9"

services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
    networks:
      - app-network

  server:
    build: ./server
    ports:
      - "4000:4000"
    env_file:
      - ./server/.env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
